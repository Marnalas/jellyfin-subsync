[pytest]
testpaths = tests
# app.py sits at the root of this directory, next to this file, and is imported
# as a top-level module both here and in the image (`uvicorn app:app`).
pythonpath = .
addopts = -q
